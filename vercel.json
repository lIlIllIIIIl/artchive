{
  "buildCommand": "npm run build --workspace=front",
  "outputDirectory": "front/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/((?!api)(?!.*\\.).*)", "destination": "/index.html" }
  ]
}

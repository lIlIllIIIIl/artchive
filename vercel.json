{
  "buildCommand": "npx vite build --config front/vite.config.ts",
  "outputDirectory": "front/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/((?!api)(?!.*\\.).*)", "destination": "/index.html" }
  ]
}
